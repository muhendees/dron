defmodule DroneImages do
	use Ecto.Model

	schema "drone_images" do
		
		field :name, :string
		field :description, :string
		field :img_file_name, :string
		field :img_file_size, :integer
		field :img_content_type, :string

		timestamps
	end
end
